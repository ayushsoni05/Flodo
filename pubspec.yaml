name: flodo_assignment
description: A clean, visually polished Task Management App.
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  # sqflite: ^2.3.0
  path: ^1.8.3
  google_fonts: ^6.1.0
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  animate_do: ^3.3.4
  uuid: ^4.3.3
  flutter_staggered_animations: ^1.1.1
  # sqflite_common_ffi: ^2.4.0+2
  # sqlite3_flutter_libs: ^0.6.0+eol

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
